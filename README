Pour récupérer la conf, cloner le dossier dans ~./vim puis faire un lien symbolique:

ln -s ~/.vim/vimrc ~/.vimrc
Then 
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
to instal plugin manager.
Then open vim and run :PluginInstall.
Finally, the plugin Commant-t requires compiling some separate libraries, go check it out on Commant-t github
